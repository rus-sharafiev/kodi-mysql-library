import { Logo } from "./Logo"

// --------------------------------------------------------------------------------

export const Header: React.FC = () => {

    return (
        <header>
            <Logo />
        </header>
    )
}